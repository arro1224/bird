# 复制模块 RC3 任务闭环（阶段 D）实施 Handoff — 2026-09-16 晚

> **状态：探索与计划已完成并获用户批准，代码零改动。** 明天从「批次 1」直接开工，无需重新探索。
> 完整计划同日已批准，全文收录于本文档第四节。

## 1. 背景与目标

依据两份联调文档（`doc/任务/` 下）：

- `01_提交给App_09-16联调问题与适配要求.md`（后端反馈）
- `v6交付包_BLE与复制功能审计_2026-09-15.md`（审计，P0-3/返修条件 2）

App 侧本轮要做（用户已确认范围 = **完整闭环（阶段 D）**）：

1. **RC3 任务闭环**（审计 P0-3）：复制任务列表、详情、进度、事件、文件明细、报告、暂停/继续/取消/失败重试、安全移除、创建任务成功后跳转任务详情。验收标准："创建任务后能进入任务详情，能看到真实状态、文件数和结果"。
2. **能力门控（已冻结负向行为）**：按 `copy-capabilities` 返回值隐藏/置灰全量备份入口（当前预检返回 `COPY_SCOPE_UNSUPPORTED`，不得进入创建流程）；元数据更新 `metadata_export_ready=false`，App 本来就没有该入口，**不得编造**。
3. **预检明细** `/api/v1/copy-previews/{preview_id}/items`：空列表不能当正常结果。
4. `UserMessageMapper` 补 `COPY_SCOPE_UNSUPPORTED`、`METADATA_EXPORT_UNAVAILABLE` 两码（现有 19 码在 `user_message_mapper.dart:98-206`）。

执行主旨（用户原话）：**打通整个复制功能模块端到端**（页面 → mock 仓库 → HTTP → mock 服务器全链路）。

## 2. 探索结论（已核实，明天不用重查）

### 2.1 copy 模块现状 `lib/bird_companion/features/copy/`

- `domain/copy_models.dart`（469 行）：CopyScope/PairPolicy/ConflictStrategy/ReviewExportConfig/StorageDeviceSummary/CopySelectionSnapshot/CopyPreview(含 previewToken, actualFileCount)/CopyRequestDraft/CopyJobSummary(仅 4 字段)/BatchTargetPreference。**发送端严格解析**（未知值抛 `ProtocolCompatibilityException`，`production_copy_b2_test.dart` 冻结该约定）。
- `data/copy_api.dart`（93 行）：devices/createSelection/preview/createJob/lastSuccessfulTarget(固定 null 桩 L79-80)/setDeviceAlias；每请求带 `X-BirdBox-Protocol: birdbox-copy-v1`（仅 POST）。
- `data/mock_copy_repository.dart`：L11 `const useMockCopyRepository = true`；5 台 mock 设备；preview/createJob 无状态。
- `presentation/copy_config_cubit.dart`：submit() L353-396 二次预检刷新 preview_token，成功后 emit createdJob + dataChanges.publish。
- `presentation/copy_confirmation_page.dart`（1090 行）：**成功弹窗 `_CopySubmitResult` L141-174 只关闭**（注释"阶段 D"）；BlocConsumer listener L201-219（onSubmitted 回调优先，demo 用）；fromArgs L39-51 非 args 非 String → media_full_backup 兜底。
- 通用 `/api/v1/jobs` 任务中心是**另一张表，不含 RC3 copy_jobs_v1 任务**（审计确认）——不能复用，须新建页面。

### 2.2 已核实的集成点（有坑，直接引用）

- **`core/network/api_client.dart:116` `get()` 没有 headers 参数**——需先加可选 `headers`（`Options(headers: headers)`，向后兼容）。post 已自动加 `X-Idempotency-Key`（L128-144），unwrapResponse 剥 `{data}` 包络。
- **`core/network/api_exception.dart`**：有 `const ApiException({required message, code, statusCode, retryable, details, cause})`——mock 里可直接 throw。
- **`repository_task_experience_controller.dart` refreshFromBox L166-201 用 `results[0..3]` 数值索引**（includeScan 影响索引）——capabilities **不能塞进 Future.wait 数组**，单独 await + catchError 吞错（失败不置 `_authorityReady=false`）。
- 端点常量已预置未使用：`core/network/api_endpoints.dart:44-55`（copyJobDetail/copyJobItems/copyJobEvents/copyJobReport/copyJobAction/storageDeviceSafeRemove）；缺 `copyCapabilities`、`copyPreviewItems`。
- `task_home_capability_resolver.dart` resolveCapabilities() 纯本地推导（L30-146）；task_experience_root.dart `_startTask()` L115-191 fullBackup 只查 canSubmitTaskWrites。
- `storage_target_page.dart`：设备卡数据来自 copyRepository.devices()，安全移除 UI 挂这里。
- mock 服务器 `tool/mock_box_server/mock_box_server.dart`：已有 GET /storage/devices(L438)、POST /copy-jobs/preview(L442)、POST /copy-jobs(L446)、_copyV1Error(L800-816)。**preview 现在对 media_full_backup 也返回成功——与真实后端 COPY_SCOPE_UNSUPPORTED 行为相反，必须改**。缺：列表/详情/items/events/report/actions/safe-remove/copy-capabilities/preview items。
- 本机 Flutter：`/d/code/flutterSetting/bin/flutter`（不在 PATH）。基线：analyze 0 问题、633 测试全过。
- 既有测试 fake：`production_copy_r3_flow_test.dart:253` `_CopyRepository implements CopyRepository`——**RC3 方法不塞进 CopyRepository 接口**，另建 `CopyJobRepository` 接口。

### 2.3 协议契约要点（从协议文档提取）

- §13.5：GET /copy-jobs/{id}（含状态、统计、设备展示快照、event_seq）；GET items?cursor=&state=failed；GET events?after_seq=；GET report。§13.6 动作 body `{expected_state_version, reason:'user_requested'}`，版本冲突 409 COPY_STATE_VERSION_CONFLICT。
- §12 状态机：draft→preflighted→queued→acquiring_target→running→(pause_requested→paused)→(cancel_requested→cancelled)→waiting_for_source/target→completed|completed_with_errors|failed。文件项：pending→copying→verifying→copied|failed、skipped_conflict、not_applicable。
- 文件项字段（§10.2）：copy_item_id/asset_id/source_media_id/source_relative_path/source_size/source_mtime_ns/target_relative_directory/target_filename/state。
- allowed_actions（迁移对照 §3.5）：pause/resume/cancel/retry_failed/safe_remove_source/safe_remove_target——详情页**只渲染后端返回的按钮**。
- 安全移除：POST /storage/devices/{media_id}/safe-remove，body `{role, expected_copy_job_id}`，safe_to_remove=true → "现在可以拔出"（保持期 60s）。
- **未冻结**（容错解析范围）：任务列表端点、详情/事件/报告完整字段、分页 cursor 形态、copy-capabilities 完整结构、preview_id 与 preview_token 关系（暂同值）。
- copy-capabilities 已知字段：supported_scopes/storage_ready/copy_ready/metadata_export_ready/features。

## 3. 已批准的关键设计决定

1. **接口拆分**：新建 `CopyJobRepository` 接口；`MockCopyRepository`/`CopyRepositoryImpl` 双 implements；DI 同一实例（`copyRepository` + `copyJobRepository`）。
2. **解析界线**：发送端保持严格 fromWire；**RC3 响应解析全容错**（未知枚举保留 raw 不崩溃、缺失给默认、未知 allowed_action 丢弃）。新模型放新文件 `copy_job_models.dart`。
3. **Mock 推进**：惰性时间推进（注入 `clock()`，读写时按相对时间推状态机），不用 Timer——测试确定性断言、详情页 2s 轮询自然驱动。
4. **capabilities 回退**：remote==null（获取失败/未部署）→ 完全回退本地推导，不劣化现状；有值严格遵循。默认值：supported_scopes 缺失→空（保守隐藏全量备份）；copy_ready/storage_ready 缺失→true；metadata_export_ready 缺失→false。
5. 通用任务中心不复用；`lastSuccessfulTarget` 桩不动（推荐盘走 devices 顶层容错字段，覆盖审计 P1-3 低成本部分）。

## 4. 实施计划（已批准，按此执行）

### 批次 1：模型层

**1.1 新建 `lib/bird_companion/features/copy/domain/copy_job_models.dart`**（容错解析，头注释声明"响应协议未冻结，未知字段忽略/缺失给默认/未知枚举保留 raw"）：

- `CopyCapabilities`：supportedScopes/storageReady/copyReady/metadataExportReady/features + `supportsFullBackup` getter。
- `CopyJobState` 常量类（非 enum）：draft/preflighted/queued/acquiringTarget/running/pauseRequested/paused/cancelRequested/cancelled/waitingForSource/waitingForTarget/completed/completedWithErrors/failed/unknown；字段 wire/label/isTerminal；getter isRunning/isWaitingDevice/isTransition。
- `CopyItemState` 常量类：pending/copying/verifying/copied/failed/skipped_conflict/not_applicable。
- `CopyAllowedAction` 常量类：pause/resume/cancel/retry_failed/safe_remove_source/safe_remove_target；fromWire 未知→null（丢弃）。
- `CopyJobStats`：totalFiles/copiedFiles/failedFiles/skippedFiles/notApplicableFiles/totalBytes/copiedBytes（缺→0）+ elapsedSeconds/etaSeconds/bytesPerSecond/currentFile（nullable）+ progressPercent。
- `CopyJobDeviceSnapshot`：mediaId/displayName/kind/userAlias 全 nullable（**不得复用 StorageDeviceSummary.fromJson**）。
- `CopyJobDetail`：copyJobId（**必填抛**）、state、stateVersion（依次试 state_version/expected_state_version/version，默认 0）、eventSeq、stats?、sourceDevice/targetDevice?、allowedActions、scopeWire?/batchId?、createdAt/startedAt/finishedAt。
- `CopyJobItem`（任务项与预检明细复用）：§10.2 字段 + conflictDecision?（copy_item_id 容错 copy_item_id|item_id|id）+ targetPath getter。
- `CopyJobItemPage`/`CopyJobPage`：{items, hasMore, nextCursor?}（next_cursor int/String 都接受）；`CopyJobEvent`/`CopyJobEventPage`（seq 容错 seq|event_seq）。
- `CopyReport`：迁移对照 §3.6 字段全 optional + isPartialSuccess（"部分成功不得显示成成功"）。
- `SafeRemoveResult`：safeToRemove（**缺→false**）、reason?、keepUntil?。
- `CopyDeviceList`：{devices, recommendedTargetMediaId?}（顶层推荐字段容错 `last_successful_target_media_id`/`recommended_target_media_id` 两键；devices 行仍严格）。

**1.2 新建 `lib/bird_companion/features/copy/domain/copy_timeline.dart`**：七阶段 `CopyTimelineStageId{waitingDevice,acquiringTarget,copyingFiles,verifyingFiles,writingReview,safeSync,done}` × `CopyStageStatus{pending,active,done,failed,skipped}`；`CopyTimeline.compute({state,stats,report})`。映射：waiting_*/draft/queued→阶段1（waiting_for_target→阶段2）；acquiring_target→2；running→3（copied>=total>0→4）；completed/completed_with_errors→1-4 done+7 done；cancelled→7 skipped；failed→当前阶段 failed。阶段5/6 运行态无信号恒 pending，终态 done——OpenAPI 冻结后在 compute 单点接入。

### 批次 2：API 与仓库层

- `core/network/api_endpoints.dart`：补 `copyCapabilities`、`copyPreviewItems='/api/v1/copy-previews/{previewId}/items'`（列表复用 copyJobs）。
- `core/network/api_client.dart:116`：get() 加可选 `Map<String, String>? headers`。
- `copy_api.dart` 新方法（全部带 `_protocolHeader`）：capabilities()/devices()（改返 CopyDeviceList）/listJobs({cursor})/getJob(id)/jobItems(id,{cursor,state})/jobEvents(id,{afterSeq})/jobReport(id)/jobAction(id,actionWire,{required expectedStateVersion})（2xx 无 copy_job_id 返 null，调用方统一重拉）/previewItems(previewId,{cursor})/safeRemoveDevice(mediaId,{required role,expectedCopyJobId})。
- 新建 `domain/copy_job_repository.dart`：abstract interface class CopyJobRepository（上述方法除 devices/setDeviceAlias）。
- `copy_repository.dart` devices() 返回类型改 Future<CopyDeviceList>；`copy_repository_impl.dart`、`mock_copy_repository.dart` 双 implements。

### 批次 3：Mock 仓库 + mock 服务器 + 错误码 + DI

- `mock_copy_repository.dart`：构造 `MockCopyRepository({DateTime Function()? clock, bool fullBackupAvailable=false})`；`_MockCopyJob`（state/stateVersion 初 1/eventSeq/phaseStart/items 固定 2 项 willFail COPY_HASH_MISMATCH 语义/events）；`_advance(job)` queued>1.2s→acquiring_target→>1.2s→running→按速率推 items→completed_with_errors；jobAction 版本不符 throw `ApiException(message:'任务状态已更新', code:'COPY_STATE_VERSION_CONFLICT', statusCode:409)`；jobItems state 过滤+cursor 分页（pageSize 20 不去重）；jobEvents afterSeq；jobReport 非终态 409；previewItems 确定性文件名+conflict_decision:'skip'；capabilities() **metadata_export_ready=false** 镜像真实后端；safeRemoveDevice busy→false/else true+keep_until 60s；devices() 返 CopyDeviceList（推荐默认 U 盘）。
- `tool/mock_box_server/mock_box_server.dart`：顶层 `const mockFullBackupAvailable=false`；内存态 `_copyJobsV1`/`_previewItemsByToken` + `_advanceCopyJob`（~6s 跑完）；新路由：GET /copy-capabilities（metadata_export_ready=false）、GET /copy-jobs 列表、GET /copy-jobs/{id}、items、events、report（非终态 409）、POST actions（版本不符 409）、GET /copy-previews/{id}/items、POST /storage/devices/{mediaId}/safe-remove。**改 _serveCopyJobPreview：media_full_backup && !开关 → 422 COPY_SCOPE_UNSUPPORTED；成功时缓存 preview items**；_createCopyV1Job 落 _copyJobsV1（含 2 willFail）。
- `app/app_dependencies.dart` L250-253：
  ```dart
  final impl = useMockCopyRepository ? MockCopyRepository() : CopyRepositoryImpl(CopyApi(apiClient));
  copyRepository: impl, copyJobRepository: impl,  // 新增 final CopyJobRepository copyJobRepository
  ```
- `user_message_mapper.dart` L196 后插两码（走 _copyMessage，后端 message 优先）：COPY_SCOPE_UNSUPPORTED→"当前盒子不支持全量备份"；METADATA_EXPORT_UNAVAILABLE→"审阅信息导出暂不可用"。

### 批次 4：RC3 页面与路由

- `app/bird_route_args.dart` 加 `CopyJobDetailArgs(copyJobId)`；`app_router.dart` BirdRoutes 加 `copyJobs='/copy-jobs'`、`copyJobDetail='/copy-job-detail'` + 两个 case（缺参落 _invalid）。
- 新建 `presentation/copy_job_list_page.dart`：RefreshIndicator+ListView+加载更多；行=状态 chip+类型（scopeWire=='media_full_backup'?'全量备份':'照片复制'）+文件进度+createdAt；onTap→详情。复用 core/widgets/empty_state、error_notice。
- 新建 `presentation/copy_job_detail_cubit.dart`：state{detail, events 累积, lastEventSeq, failedItems 累积(不前端去重), report?, loading, acting, error}；load()（getJob+events+终态 report+failedItems 首页）/pollTick()（幂等跳过 loading）/loadMoreFailedItems()/act(action)。事件缺口（首 seq>afterSeq+1）→afterSeq=0 重拉一次（仍 gap→error 防死循环）；版本冲突→自动 load()+"任务状态已更新，已刷新"。
- 新建 `presentation/widgets/copy_job_timeline.dart`（渲染 compute 结果，复用 TaskSectionTitle）。
- 新建 `presentation/pages/copy_job_detail_page.dart`：TaskNatureBackground+AppBar；等待态横幅（waiting_for_source→"等待相机卡：请将源相机卡插回盒子，任务将自动继续"；waiting_for_target→"等待原目标 U 盘：请重新插入同一目标设备，不会自动更换其他设备"；pause_requested→"当前文件完成后暂停"；cancel_requested→"已完成的副本不会删除"）；进度卡（0/null 不显示——后端 P1-5 当前假值）；时间线；设备快照；事件列表（倒序上限 50）；**动作区只渲染 allowedActions**（pause 带"当前文件完成后暂停"、cancel 确认"已完成的副本不会删除"、safe_remove_*→确认→safeRemoveDevice→"现在可以拔出（保持期 60 秒）"或 SnackBar mapper）；失败项分页（cursor 加载更多）；终态报告卡（isPartialSuccess→warning 色"部分完成（有失败项）"，不得用成功样式）；轮询（运行态 2s/waiting 5s/终态取消）+AppLifecycleListener+手动刷新。
- 新建 `presentation/widgets/copy_preview_items_sheet.dart`：行=源路径→目标路径+大小+conflictDecision 徽标（skip→跳过/overwrite→覆盖/keep_both→两份保留）；**核心验收点：items.isEmpty && summaryFileCount>0 → Key 'copy-preview-items-unavailable' amber 警告"预检明细暂不可用…不影响任务创建"，不得渲染成"暂无文件"正常空态**。
- 改 `copy_confirmation_page.dart`：确认步骤加"查看预检明细（N 个文件）"TextButton → Sheet（loadPage 用 previewItems(state.preview!.previewToken)）；`_CopySubmitResult` 加主按钮 Key `'copy-submit-result-open-progress'`"查看任务进度"→pop(true)；listener 改 showDialog\<bool\>：true→pushNamed(copyJobDetail, CopyJobDetailArgs) 返回后关复制页，否则关复制页；保留 onSubmitted 优先分支。

### 批次 5：入口与能力门控

- `task_home_capability_resolver.dart`：加 `CopyCapabilities? remote`——copy：enabled = hasPhotos && !targetWriteBusy && (remote==null || remote.copyReady)，!copyReady→"盒子复制功能暂不可用"；fullBackup：enabled = cardPresent && !targetWriteBusy && (remote==null || (remote.supportsFullBackup && remote.copyReady))，!supportsFullBackup→"当前盒子不支持全量备份"；remote==null 完全回退本地。
- `repository_task_experience_controller.dart`：构造加可选 `CopyJobRepository? copyJobRepository`（既有测试零改动）；**capabilities 单独 await + catchError 吞错，不进 Future.wait 数组（results 数值索引坑）**，失败不置 _authorityReady=false；taskHomeCapabilities 传 remote。
- `task_experience_root.dart`：controller 传 copyJobRepository；`_openCopyJobs()`（push copyJobs，返回后 refreshFromBox）；`task_home_page.dart` "任务列表"标题行（L53-54）加右侧 TextButton.icon('复制任务')。
- `copy_config_cubit.dart`：state 加 scopeUnavailable；submissionBlockReason 最前命中（禁提交）；fetchPreview catch `ApiException.code=='COPY_SCOPE_UNSUPPORTED'`→emit scopeUnavailable=true+clearPreview；setScope 清除。`copy_confirmation_page.dart` 内容步骤插 Key `'copy-scope-unavailable-notice'` amber 卡"当前盒子不支持全量备份…本次不会创建任务"+下一步禁用。
- `storage_target_page.dart`：设备卡（canBeTarget&&online）加"安全移除"→确认→safeRemoveDevice(role:'target')→"现在可以拔出（保持期 60 秒）"或 SnackBar。

### 批次 6：测试（基线 633 全绿 + analyze 0）

| 文件 | 覆盖 |
|---|---|
| `test/.../copy/copy_job_models_wire_test.dart`（新） | 未知 state 不抛/三键 stateVersion/stats 缺失默认/未知 allowed_action 丢弃/capabilities 缺失默认/next_cursor int→String/safe_remove 缺→false/CopyDeviceList 两键 |
| `test/.../copy/mock_copy_job_lifecycle_test.dart`（新） | 注入可变 clock：全状态序列/pause→paused→resume/cancel/retry_failed/版本冲突 409/items failed 过滤+分页不去重/events 增量/report 非终态 409/safeRemove busy→safe/listJobs 分页 |
| `test/.../tasks/production_task_b1_capability_test.dart`（扩） | remote 合并三态 |
| `test/.../copy/production_copy_scope_unsupported_test.dart`（新） | preview 抛 COPY_SCOPE_UNSUPPORTED→scopeUnavailable=true、submit 不调 createJob |
| `test/.../copy/copy_submit_result_dialog_test.dart`（新） | widget test：弹窗→tap open-progress→push copyJobDetail 携 args；关闭→pop |
| `test/.../copy/copy_preview_items_sheet_test.dart`（新） | 空明细+count>0→警告 Key 存在且无正常空态；行渲染；加载更多 |
| mapper 新码 | 并入上述任一文件 |
| `test/bird_companion/core/mock_box_server_test.dart`（扩，若存在） | capabilities 负向值/preview 全量备份 422/创建→轮询 completed_with_errors/动作版本冲突 409/safe-remove |

验证：`/d/code/flutterSetting/bin/flutter analyze`、`/d/code/flutterSetting/bin/flutter test`。

### 明确不做（范围外）

元数据更新入口/页面（不编造）；BLE；通用 job center 改造；lastSuccessfulTarget 独立端点。

### 联调期只调解析层的单点清单（后端 OpenAPI 冻结后）

Page 分页字段名→三个 Page factory；设备快照字段→CopyJobDeviceSnapshot；时间线阶段 5/6 信号→CopyTimeline.compute；动作响应体→jobAction nullable+统一重拉；preview_id/preview_token 关系→CopyApi.previewItems；新等待态文案→详情页横幅单点。

## 5. 明日开工清单

1. 按批次 1→6 顺序执行，每批结束跑 analyze；批次 4 结束即可手动走通"创建→详情"闭环。
2. 全部完成后：写实施记录到本目录（对齐 `拍鸟伴侣_App复制功能迁移实施记录_A-B-C阶段_2026-09-09.md` 格式）；更新记忆 `birdapp-copy-migration.md`（阶段 D 完成、mock 开关、联调待办）。
3. 回传联调材料（01 文档 §三）：复制任务请求/响应摘要、job_id、最终状态、失败项及错误码——mock 阶段先用 mock 服务器日志。
